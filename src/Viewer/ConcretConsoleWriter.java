package Viewer;

import controller.ConsoleWriter;
import javafx.scene.Node;

public class ConcretConsoleWriter implements ConsoleWriter{
    private Node console;

    public ConcretConsoleWriter(Node console) {
    }

    @Override
    public void write(String text) {
    }
}
